#ifndef UTILS_H
#define UTILS_H

#include <string>
#include <vector>

// Example utility: convert decimal string to 32-byte target
std::vector<uint8_t> decimal_to_target_bytes(const std::string& decimal);

#endif // UTILS_H
